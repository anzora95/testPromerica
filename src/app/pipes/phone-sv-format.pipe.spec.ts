import { PhoneSvFormatPipe } from './phone-sv-format.pipe';

describe('PhoneSvFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneSvFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
